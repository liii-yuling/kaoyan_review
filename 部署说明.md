# 部署说明（照着做一次，以后只用重复第 4 步）

本文面向不写代码的人。**只需要完整读一次，做一次。** 之后每次更新只重复最后一步。

---

## 📌 你的部署信息（已确认，直接照用）

| 项目 | 值 |
|---|---|
| GitHub 用户名 | `liii-yuling`（**三个 i**） |
| 仓库名 | `kaoyan_review` |
| **她要用的网址** | **`https://liii-yuling.github.io/kaoyan_review/`** |

> ⚠️ 用户名是**三个 i**（`liii`），不是两个。写错一个字母她那边就打不开。
> 这个网址已经填进网站的「运营台」页，那页也能一键复制。

**已经帮你验证过的两件事：**

1. 网站所有资源都是**相对路径**（41 个引用无一例外），所以放在 `/kaoyan_review/` 这种子路径下**能正常打开**——我用本地服务模拟了完全一样的目录结构，41/41 个文件全部 200。
2. 网站代码里有 `{{` 这种符号，但只出现在 `.js` 的注释里。GitHub Pages 的 Jekyll 只解析 `.md` 和 `.html`，`.js` 是原样透传，**不会造成构建失败**。

---

## 一、先搞清楚三件事

### 1. 为什么必须放到"静态托管"上？

你的要求是：**"我电脑关机也不影响他使用"**。

这意味着文件不能只存在你的电脑里。只要文件在你电脑上，你关机他就打不开——这是物理限制，
不是技术选型问题。所以必须把网站文件放到一台**一直开着的机器**上。

"静态托管"就是干这个的：**免费、不需要租服务器、不需要备案**（用平台自带的网址的话），
你要做的只是把文件夹拖上去。

> 顺带说明：你之前看到的 `http://127.0.0.1:8788/` 只对你自己有效。
> `127.0.0.1` 的意思是"我自己这台电脑"，发给别人是打不开的。

### 2. 为什么不能直接把网址发过去就完事？

因为**浏览器缓存**。静态文件（`.js`/`.css`）会被浏览器存起来。
如果你只是覆盖上传了新文件，对方刷新时浏览器可能还在用旧文件——他会觉得"你没更新"。

**本项目已经解决了这个问题**，你不需要懂原理，只要知道：

- 用 `发布.cmd` 打包时，会给每个文件加上版本号（`?v=20250601-153015`），版本号一变 URL 就变，缓存必然失效；
- 页面启动时会去比对服务器上的 `version.json`；
- **对方那边会自己弹出一条「有新版本可用，点此刷新」的提示条**，点一下就是新版。

### 3. 对方的数据在哪里？

**在他自己的浏览器里**（localStorage），不在服务器上，也不在你这里。

- 好处：你更新网站，**他的错题本、掌握度、练习记录全都不受影响**；
- 代价：**你这边看不到他的数据**，他也看不到你的。数据不共享。
- 另外：他如果换浏览器、换电脑、清理浏览器数据，**数据会丢**——所以让他定期用
  「设置与备份 → 导出完整备份」。

---

## 二、选哪个平台

| 方案 | 优点 | 缺点 | 建议 |
|---|---|---|---|
| **A. GitHub Pages** | 免费、永久、网址稳定、不用实名、更新方便 | 国内访问速度一般，有时偏慢 | **首选**，先试这个 |
| **B. Netlify Drop** | 最快，拖一下就有网址，注册都不用 | 国内访问经常不通 | 只想 30 秒看到效果时试 |
| **C. 腾讯云 CloudBase 静态托管** | 国内速度最好 | 需要**实名认证**，有免费额度 | GitHub 太慢时换这个 |
| D. Vercel | 免费、好用 | 国内访问经常不通 | 不推荐给国内使用 |

> 我给不出"国内一定能秒开"的免费方案——那类服务基本都要实名或备案。
> **建议先做 A 方案，让对方实测一下速度**；如果太慢，再上 C 方案。
> 三个方案打包出来的 `dist/` 是同一个东西，换平台只需重新上传，不用改代码。

---

## 三、方案 A：GitHub Pages（推荐）

### 第 0 步：本地确认没问题

1. 双击项目根目录的 `发布.cmd`
2. 按提示输入更新说明（可以随便写，也可以直接回车）
3. 打包完，双击 **`dist` 文件夹里的** `启动.cmd`
4. 浏览器打开后随便点几下，确认页面正常
5. **确认完请关掉那个服务窗口**（不关也能重新发布，但你可能会看到一条
   "dist 目录本身删不掉"的提醒，那是正常的，发布程序会自动退而求其次只清空内容）

> 打包时会先在本地跑三套自检（题库格式、核心流程、页面渲染），
> 任何一套不通过都会中止发布并告诉你哪里有问题——避免把坏版本发出去。

### 第 1 步：注册 GitHub 账号

打开 <https://github.com/signup>，用邮箱注册，免费。**不需要实名。**

### 第 2 步：建一个仓库

1. 登录后点右上角 `+` → **New repository**
2. **Repository name** 填 `kaoyan_review`（和你已经建好的那个一致）
3. 选 **Public**（必须是 Public，Private 的 Pages 要付费）
4. 其他都不用动，点 **Create repository**

### 第 3 步：上传网站文件

#### 方式一：双击「推送.cmd」（★ 推荐）

1. 双击项目根目录的 **`发布.cmd`**（先打包）
2. 双击项目根目录的 **`推送.cmd`**
3. **第一次**会弹出一个浏览器窗口让你登录 GitHub 并授权 —— 点同意，**只需这一次**
4. 看到「推送成功」就好了

以后每次更新：`发布.cmd` → `推送.cmd`，两次双击，完事。

**为什么推荐这种方式：**

- 不用手动拖文件，不会出现"把 `dist` 文件夹本身传上去"这种错
- 隐藏文件（`.nojekyll`）也会正确推上去，网页拖拽常常会漏掉
- 只传变化的文件，快
- 脚本会自动接好远程仓库，你不需要懂 git

> ⚠️ 这条路径用的是"强制覆盖"：**这个仓库只用来放网站，不要在 GitHub 网页上直接改里面的文件**，
> 否则下次推送会把它覆盖掉。

#### 方式二：网页手动拖拽（不想装/不想用 git 时）

1. 在仓库页面点 **Add file** → **Upload files**
2. 打开本项目根目录下的 `dist` 文件夹
3. **把 `dist` 里面的所有东西全选，拖进上传区**（可以按 `Ctrl+A` 全选）

   > ⚠️ **最容易搞错的一步**：要拖 `dist`**里面**的内容，
   > **不要**把 `dist` 这个文件夹本身拖进去。
   > 拖对了的话，上传列表里第一层应该能看到 `index.html`；
   > 如果只看到一个 `dist` 文件夹，那就拖错了，网站会打不开。

4. 下方填一句提交说明（随便写），点 **Commit changes**
5. 隐藏文件 `.nojekyll` 拖不上去没关系，不影响使用

### 第 4 步：打开 Pages

1. 仓库页面点 **Settings**（顶部）
2. 左侧菜单找到 **Pages**
3. **Source** 选 `Deploy from a branch`
4. **Branch** 选 `main`，右边的目录选 `/ (root)`
5. **如果 `Save` 按钮是灰的**，说明已经保存过了——直接跳到第 6 步
6. 等 **1~2 分钟**，刷新这个页面，顶部会出现网址：

   ```
   **`https://liii-yuling.github.io/kaoyan_review/`**
   ```

   这个就是**发给对方的网址**。

### 以后怎么更新（只需两次双击）

1. 双击项目根目录的 **`发布.cmd`** —— 它会先跑一遍自检，再打包出新的 `dist`
2. 双击 **`推送.cmd`** —— 自动提交并推送（第一次之后不再需要登录）
3. 等 1~2 分钟。她**刷新页面**，顶部会出现「有新版本可用」提示条，点「立即刷新」就是新版

> 如果你的仓库是用**网页拖拽**方式维护的（没用过 `推送.cmd`），那就把第 2 步换成：
> 仓库 → **Add file** → **Upload files** → 把 `dist` 里的内容再全选拖进去 → **Commit changes**。

> GitHub 对网页本身（`index.html`）有约 **10 分钟**的缓存。
> 所以万一她刷新后没看到提示条，让她等 10 分钟再刷新一次即可。
> 这一点 `README.md` 和设置页里都写了，她不会以为你没更新。

> **改前面的内容后必须重新发布**：`发布.cmd` 会给所有资源加上新的版本号
> （`app.css?v=20260916-203200` 这种）。不加版本号，浏览器会用缓存里的旧文件，
> 她刷新也看不到你的改动。所以**永远不要跳过 `发布.cmd` 直接推 `dist`**。

---

## 四、方案 B：Netlify Drop（30 秒看效果）

1. 双击 `发布.cmd` 打包
2. 打开 <https://app.netlify.com/drop>
3. 把 **`dist` 文件夹本身**拖进页面中间的虚线框（这个平台要拖文件夹）
4. 几秒后得到一个网址，形如 `https://随机名字.netlify.app`
5. 这就是网址，可以直接发给对方

**注意**：不注册账号的话，关掉页面后就找不回这个站点了，也没法更新。
要长期用就得点页面上的 **Claim your site** 注册一个免费账号。

**国内访问可能不通。** 如果对方打不开，别在这里浪费时间，回方案 A 或 C。

---

## 五、方案 C：腾讯云 CloudBase 静态托管（国内速度快）

适合 GitHub Pages 太慢的情况。

1. 注册腾讯云账号并完成**实名认证**（这一步躲不掉，是法规要求）
2. 进入**云开发 CloudBase** 控制台，创建一个环境（选按量计费，有免费额度）
3. 左侧找到**静态网站托管**
4. 双击 `发布.cmd` 打包，把 **`dist` 里的全部内容**上传到根目录
5. 平台会给你一个默认域名，形如 `https://xxx.tcloudbaseapp.com/`，用这个当网址

**成本**：个人学习用途的流量极小，基本落在免费额度内。不放心就先看它的计费说明。

---

## 六、必须知道的注意事项

| 事项 | 说明 |
|---|---|
| **网址一旦定下来就别改** | 数据是按"网址"隔离存储在对方浏览器里的。你把仓库改名、换平台，网址就变了，对方之前的数据在新网址下**读不到**（旧数据还在，只是回不去） |
| **数据不共享** | 你这边看不到他的错题和成绩。要共享得加后端数据库，那是另一个工程 |
| **让对方定期导出备份** | 换浏览器、清理缓存都会丢数据。让他每周「设置与备份 → 导出完整备份」 |
| **不要上传 `dist` 文件夹本身** | 要上传它**里面**的内容，第一层必须能看到 `index.html`（方案 B 例外，那个平台要拖文件夹） |
| **更新后有 10 分钟延迟是正常的** | 平台侧的 HTML 缓存。对方多刷新几次，或等一会儿 |
| **重新发布前最好关掉本地服务窗口** | 如果你在 `dist` 里跑过 `启动.cmd`，那个窗口会占住 `dist` 文件夹。发布程序会提示并自动绕开，但关掉更干净 |
| **`robots.txt` 已禁止收录** | 打包时自动生成，避免这个私人复习工具被搜索引擎搜到 |
| **`.nojekyll` 拖不上去也没关系** | 它是隐藏文件（以点开头），网页上传时可能被忽略。少了它网站照样能用——只会让 `_headers` 这个文件不被放出去，而 GitHub 本来也不读它。真想补上：仓库页 → Add file → Create new file → 文件名填 `.nojekyll` → 内容留空 → Commit |
| **图片识别首次要联网** | 本地 OCR 引擎首次使用时需要联网下载约 15MB 语言包，之后浏览器会缓存 |

---

## 七、打包时到底做了什么（想懂原理可以看，不想看可跳过）

双击 `发布.cmd` 会依次做四件事：

1. **跑三套自检**——题库格式校验、核心流程自测、页面渲染测试。任一不通过就中止，不会把坏版本发出去。
2. **拷贝一份干净的 `dist/`**——只含网站真正需要的东西，不含 `tools/` 这类开发脚本。
3. **给每个资源加上版本号**——`assets/css/app.css` 变成 `assets/css/app.css?v=20250601-153015`。
   版本号一变，URL 就变，浏览器缓存必然失效。**这是"对方刷新就能拿到新版"的技术关键。**
4. **写 `version.json` 和平台缓存配置**——页面启动时去读 `version.json` 比对，
   发现不一致就在页面顶部弹出「有新版本可用，点此刷新」提示条；同时生成
   `_headers` / `netlify.toml` / `vercel.json`，让各托管平台把 `index.html` 和
   `version.json` 设成"每次回源校验"，把带版本号的资源设成"永久缓存"。

一句话总结：**资源靠版本号强制换新，入口靠缓存配置保证及时，用户靠提示条知道该刷新了。**

---

## 七、常见问题

**Q：对方打开是空白页 / 404？**
A：九成是第 3 步上传错了——把 `dist` 文件夹本身传上去了。检查仓库根目录下是否**直接**有 `index.html`。
如果是 404，再确认 Settings → Pages 里 Branch 选了 `main` 和 `/ (root)`。

**Q：对方说打不开网址？**
A：先确认你给他的网址结尾有没有斜杠，两种都试试。如果国内打不开 `github.io`，换方案 C。

**Q：我更新了，对方刷新还是旧版？**
A：让他等 10 分钟；或者按 `Ctrl + F5` 强制刷新。页面顶部出现「有新版本」提示条时点「立即刷新」最稳。

**Q：对方的数据会不会因为更新丢？**
A：不会。只要网址没变，浏览器就认它是同一个网站，数据继续用。

**Q：能不能不用命令行？**
A：可以。`发布.cmd` 和 `启动.cmd` 都是双击运行的，不需要敲命令。

**Q：能不能让对方也能上传题目？**
A：可以，他在自己的浏览器里用「题库导入」页上传的题只有他自己看得到（数据在本地）。这是设计如此。

---

## 八、我更新了题库，对方怎么拿到

**先说最容易搞错的一点：** 你在网站的「题库导入」页上传题库后点「导入这 N 题」，
题目只进了**你自己浏览器的本地存储**——对方是拿不到的。这是有意的：每个人可以有自己的题库。

要让对方也拿到，题库必须变成**网站文件的一部分**，跟着代码一起发布。所以题库分三层：

| 层 | 在哪 | 对方能看到吗 |
|---|---|---|
| 内置题库 | 程序自带 | ✅ |
| **共享题库** | `src/data/bank.shared.js` | ✅ **刷新页面就拿到** |
| 个人题库 | 浏览器本地 | ❌ 只有你自己 |

合并优先级：**内置题 > 共享题 > 个人题**。

### 怎么发布共享题库（两条路，选一条）

**路径 A：导出文件（推荐）**

1. 打开网站 →「题库导入」页
2. 上传你的题库（CSV/Excel 或 JSON）→ 点「解析并预览」→ 核对无误
3. 点预览区右边的「**导出为共享题库文件**」→ 下载得到 `bank.shared.js`
4. 用这个文件**覆盖**项目的 `src/data/bank.shared.js`
5. 双击 `发布.cmd` → 把 `dist` 里的内容重新上传
6. 对方刷新页面 → 顶部出现「有新版本」→ 点刷新，新题库到手

**路径 B：放一个 JSON，让发布脚本自动转换（不用进 src 目录）**

1. 「题库导入」页 → 点「**导出 JSON 到项目根目录**」→ 得到 `共享题库.json`
2. 把它放到项目根目录（和 `发布.cmd` 同一层）
3. 双击 `发布.cmd` —— 脚本会自动读它、生成共享题库内容、打包进 `dist`
4. 上传 `dist`

路径 B 是**整体替换**：每次发布会用那个 JSON 完全决定共享题库内容。
要往共享题库里**追加**题目，先把旧 JSON 在导入页重新上传，和新题一起导出。

### ⚠ 一个必须注意的坑：题目 id 不要重复

如果共享题库里某道题的 `id` 和内置题库里的题重复了，**这道题不会生效**
（内置题优先级更高），而且它会安静地消失——你会以为发出去了，其实对方看不到。

系统会在两个地方明确告诉你：
- 「题库导入」页的共享题库卡片会弹**红色警告**并列出冲突的 id；
- `发布.cmd` 会打印共享题库题数，你可以和你的题库总数核对。

**建议**：自己加的题统一加个前缀，比如 `my-`，就不会和内置题撞。

### 对方的数据会受影响吗

不会。共享题库更新时，对方的**错题本、掌握度、练习与考试记录完全不受影响**——
那些数据是按**考点**关联的，不是按具体题目。只有题目本身会被更新。

---

## 九、我更新了每日计划和错题推送

这两样和题库一样，都是"发布出去"的东西，流程完全相同。**都在网站的「运营台」页完成。**

### 每日计划

1. 打开网站 →「运营台」→ ① 每日计划 → 点「填入示例」看格式
2. 按格式手写，例如：

   ```
   # 第 1 天 2025-06-01 打基础
   - 数学一 极限计算 20 题 @60
   - 英语一 阅读 2 篇 @50
   - 政治 马原 唯物辩证法 @30

   # 第 2 天
   - 考试 2019 年英语一真题 @180
   ```

3. 点「**解析预览**」——这一步很重要：表格里会显示每项任务被识别成什么类型、
   挂了哪个科目、哪个考点、几道题、她那边会看到什么按钮。**自动推断的结果要核对**，
   不对就在文字里把科目名/考点写清楚，或用方括号写死。
4. 点「导出计划文件」→ 得到 `plan.shared.js` →
   **更省事**：点「导出 JSON 到项目根目录」→ 得到 `共享计划.json`，放进项目根目录
5. 双击 `发布.cmd` → 上传 `dist`

她刷新后就能在「今日计划」里看到今天该做什么，逐项打勾；刷题任务点「去刷题」直接开练，
考试任务点「开始考试」直接开考。**她的打勾进度存在她自己浏览器里**，你更新计划不会清掉它。

### 错题推送

1. 「运营台」→ ② 错题推送
2. 用上面的搜索框在题库里找题，点「加入」把题目 id 追加到清单；
   也可以直接手写（每行「题目 ID 或题干关键词 | 你想说的话」）
3. 点「校验清单」核对
4. 「导出推送文件」→ `push.shared.js`，或「导出 JSON 到项目根目录」→ `共享错题推送.json`
5. 双击 `发布.cmd` → 上传 `dist`

她那边会出现「布置给你的错题」，点「全部收下」这些题就进她自己的错题本并参与复习推送。

> ⚠ **推送是"她收下"而不是"你写进去"。** 她的错题本在她自己浏览器里，你碰不到。
> 所以流程是：你发布清单 → 她点一下收下。她没收下之前，清单一直挂在她的「今日计划」页上。

### 发布清单里的坑

| 情况 | 后果 | 怎么办 |
|---|---|---|
| 计划里写了科目但认不出考点 | 她看到任务，但点「去刷题」只能按整科推题 | 在任务文字里写清考点名，或用 `[math1\|m1.p.limit.eval]` 写死 |
| 考试任务没认出套卷 | 她点「开始考试」只能进组卷页 | 用 `[paper:套卷id]` 写死（套卷 id 在运营台 ⑥ 里能看到） |
| 改动了任务的文字 | 那一项的打勾会被重置 | 改文字是可以的，只是她会重新勾一次 |
| 调整任务顺序 | **没有影响**，打勾不会错位 | 放心调 |

---

## 十、上传视频资料和题目资料（资料库）

### 先明确一件事：**视频文件不能传进这个仓库**

| 限制 | 数值 |
|---|---|
| **单个文件上限** | **100 MB**（GitHub 硬性，超了直接拒绝） |
| 仓库建议大小 | < 1 GB |
| Pages 站点上限 | 1 GB |
| 服务条款 | **明确禁止把仓库当大文件存储/CDN 用** |

一集 45 分钟的考研网课通常 **300 MB ~ 1 GB**，**一集就超了**。
硬传的后果是仓库被限制、站点被关、严重时封号。
而且仓库是 Public，放公开的大机构网课视频，被投诉下架的概率很高。

**正确做法：视频留在网盘，网站里放"资料库"入口。**

### 怎么做（在「运营台 → ④ 资料库」）

1. 打开网站 →「运营台」→ ④ 资料库 → 点「填入示例」看格式
2. 按这个格式粘贴你的网盘链接：

   ```
   # 数学一
   视频 | 张宇高数基础班 第1讲 极限 | https://pan.baidu.com/s/1abc | 提取码: abcd | 45分钟 | 先看这个
   文档 | 高数讲义 第一章 | https://pan.baidu.com/s/1def | 提取码: 1234

   # 英语一
   视频 | 唐迟阅读基础 | https://pan.quark.cn/s/xyz | 1小时20分
   ```

   系统会自动识别：**科目、类型（视频/文档）、网盘平台、提取码、时长**，并统计总时长。

3. 点「**解析预览**」核对（表格里能看到每条的科目、平台、提取码、时长对不对）
4. 点「导出资料库文件」→ `resources.shared.js`；
   **更省事**：点「导出 JSON 到项目根目录」→ 得到 `共享资料库.json`，放进项目根目录
5. 双击 `发布.cmd` → 双击 `推送.cmd`

她那边打开「资料库」就能按科目浏览，**提取码点一下自动复制**，
点「打开链接」直接跳网盘。

### 关于题目资料

你说的是**图片/截图、Word/PDF 讲义、纸质书要拍照**——这些分两种处理：

| 资料形态 | 怎么处理 |
|---|---|
| **讲义、卷子、PDF 原件** | 走上面的**资料库**（放网盘，站里放链接） |
| **想变成"能刷、能判分、能进错题本"的题** | 用「**题库导入 → 真题原文（整卷粘贴）**」，见下面 |
| 零散的题目截图 | 走「**错题本 → 上传错题**」，拍照 OCR → 自动归类考点和错因 |

### 怎么把一整年的真题录进题库

**「题库导入」页 → 切到「📝 真题原文（整卷粘贴）」**，然后：

1. 选**科目**、填**年份**和**来源**（例如「2020 年考研数学一真题」）
2. 把真题原文**整卷粘进文本框** —— 从 PDF / Word / 网页里直接复制就行，不用整理格式
3. 如果卷子的答案统一印在末尾，把那部分**贴到「答案区」**（支持 `1-5 ABCDB 6-10 ACBDA` 这种区间写法）
4. 英语阅读如果文章和题目是分开的，把文章贴到「**阅读材料**」框里
5. 点「解析并预览」→ 逐题核对 → 勾上「**并入已发布的共享题库**」→ 导出 → 发布 → 推送

**能自动识别的写法：**

| | 认得的格式 |
|---|---|
| 题号 | `1.` `2、` `（3）` `第4题` |
| 选项 | `A.` `A、` `（A）` `A)` |
| 答案 | `答案：D` `【答案】C` `正确答案 D`，或贴在答案区 |
| 解析 | `解析：…` `【解析】…`（可多行） |
| 分节 | `一、单项选择题` `Section I`（用来自动判断板块） |
| 阅读材料 | `【文章】` … `【题目】` |

**两个必须知道的点：**

- **「并入已发布的共享题库」默认是勾上的，别取消**。逐年录真题时，取消勾选会让你这一次的题**覆盖掉之前发布的所有题**。
- 录进来的题会自动归类考点；年份会让它自动进入「历年真题」的真题墙并按年份成卷。

> ⚠️ **不要指望我能替你生成真题。** 我没有真题原文，凭记忆写只会产出"看起来像真题的错误内容"，
> 她会把错的背进去。所以这个工具是用来**把你手上的资料快速变成题库**的，资料还得你提供。

### 大批量图片/照片录入

整本书的题目照片**还不能一次批量导入**（现在只能一张一张走"上传错题"）。
如果你要这个，告诉我，我来加"批量图片 → OCR → 逐题核对 → 进题库"的流程。

---

## 十三、运营台快捷方式（推荐先做这个）

运营台是你日常用的页面，值得放一个桌面图标。

**双击项目根目录的「创建桌面快捷方式.cmd」**，会在桌面建三个图标：

| 图标 | 双击后 |
|---|---|
| **考研运营台** | 启动本地服务并**直接打开运营台**（推荐） |
| **考研运营台（线上）** | 用浏览器打开线上运营台（不用起本地服务） |
| **考研复习系统** | 打开系统首页 |

如果双击后桌面没出现图标，手动做一次也很简单：
**右键项目根目录的「运营台.cmd」→ 发送到 → 桌面快捷方式**。

### 两个运营台，用哪个？

| | 本地运营台 | 线上运营台 |
|---|---|---|
| 网址 | `http://127.0.0.1:8788/#/console` | `https://liii-yuling.github.io/kaoyan_review/#/console` |
| 需要 | 双击 `运营台.cmd`（电脑上要有 Node） | 只要能上网 |
| 数据 | 存在本机这个网址下 | 存在浏览器里**另一个**网址下 |

⚠️ **这两个网址的数据是分开的**（浏览器按网址隔离存储）。
对你来说影响不大——运营台的主要工作是"编辑 → 导出文件 → 发布"，导出的是文件，跟网址无关。
但如果你在「题库导入」页往**个人题库**里传过题，那些题只存在当时那个网址下。

**建议固定用一个**，别来回切，免得找不到自己的东西。

---

## 十四、访问口令（登录）—— 先说清楚它挡得住什么

站里有一个登录门，初始账号密码都是 `yxt123`，可以在「运营台 → ⑤ 访问口令」里改。

### ⚠️ 它只挡得住"随手打开网址的人"

| 挡得住 | 挡不住 |
|---|---|
| 搜索引擎爬虫 | 会按 F12 打开开发者工具的人 |
| 误点链接的陌生人 | 直接读源码的人（仓库是 Public，代码全公开） |
| 随便试网址的人 | 手动改浏览器存储的人 |

**为什么挡不住**：这个网站没有服务器，所有验证都在她的浏览器里跑。
代码是公开的，口令只是比对了一个哈希值——懂技术的人可以直接绕过。

**顺带说一件你提到的事**：纯前端的"**注册**"功能**没有意义**——
任何人都能注册就等于没有门。所以这里只做"固定账号 + 口令"，不做注册。

### 需要真正安全的访问控制？两条路

| 方案 | 成本 | 强度 |
|---|---|---|
| **Cloudflare Pages + Cloudflare Access** | 免费（50 用户以内） | ✅ 真正的服务端鉴权（邮箱验证码），未登录的人连 HTML 都拿不到 |
| 把仓库改成 Private | GitHub Pro 约 $4/月 | ✅ 服务端鉴权，但**不再免费** |

**Cloudflare Access 大致流程**（要做的话我可以帮你准备配置文件）：
1. 把 `dist` 部署到 Cloudflare Pages（免费，和 GitHub Pages 类似，拖上去即可）
2. 在 Cloudflare Zero Trust 里建一个 Access 应用，指向你的 Pages 域名
3. 加一条策略：只允许她的邮箱登录（会发验证码）
4. 之后她打开网址会先看到 Cloudflare 的登录页，未通过的人连网站文件都下载不到

### 改口令

「运营台 → ⑤ 访问口令」→ 填新账号密码 → 「导出新的口令文件」→
覆盖 `src/data/auth.shared.js` → `发布.cmd` → `推送.cmd`。

**更省事**：点「导出 JSON 到项目根目录」→ 得到 `共享口令.json` → 放进项目根目录 → `发布.cmd`
（发布脚本会自动转换；这个 JSON 里可以直接写明文 `pass`，脚本会用同样的算法算成哈希）。

**一改口令，所有已经登录的设备都会立刻被重新拦下**（包括你自己的手机），
需要用新口令重新登录。

> 口令不会明文存在文件里，只存 `sha256(账号 + 口令 + 盐)`。
> 但**建议别用 `yxt123`**——太短，哈希虽然不可逆，这种短密码一秒就能猜出来。

---

## 十二、给她的原话（可以直接复制发给她）

> 这是一个给你做的考研复习网页，用这个网址打开就行，不用装任何东西：
> **https://liii-yuling.github.io/kaoyan_review/**
>
> 几点说明：
> 1. 打开后先看「**今日计划**」，那是我给你排的每日任务，做完一项勾一项。
>    标了「去刷题」的点一下直接进对应考点的练习；标了「开始考试」的点一下直接开考，限时自动判分。
> 2. 我有时候会给你**布置几道错题**，会在那一页显示，点「全部收下」就进你自己的错题本，
>    之后系统会自动盯着这些考点给你推题。
> 3. 你的错题本、掌握度、打勾进度、考试成绩都**存在你自己的浏览器里**，不会上传，我也看不到。
>    所以：别用无痕模式打开，也别清理浏览器数据——会丢。
>    建议每周点一次「设置与备份 → 导出完整备份」存个文件。
> 4. **我更新之后你只需要刷新页面**：顶部会出现「有新版本可用」，点一下「立即刷新」就是新版，
>    你的数据不会丢。如果没看到提示条，等 10 分钟再刷一次（GitHub 有缓存）。
> 5. 英语一的每道真题都有「查看答案与详解」按钮，点开会展开答案、解析和 B站视频。
> 6. 你也可以自己在「考试模式」里智能组卷，不一定要等我安排。
> 7. 有问题随时跟我说，但你浏览器里的数据我这边看不到，需要的话你把备份文件发我。
